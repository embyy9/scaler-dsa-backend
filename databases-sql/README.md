# Module 8 — Databases & SQL
**Topic:** Relational Databases
**Status:** 🔒 Upcoming

## Package Structure

```
queries/
├── basics/         # SELECT, WHERE, ORDER BY, GROUP BY
├── joins/          # INNER, LEFT, RIGHT, FULL joins
├── subqueries/     # Nested queries, correlated subqueries
├── aggregations/   # COUNT, SUM, AVG, window functions
└── schema/         # Table design, normalization, indexes
```
